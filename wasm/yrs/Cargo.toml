[package]
name = "yrs"
version = "0.1.0"
edition = "2024"

[lib]
name = "my_yrs_lib"
path = "src/lib.rs"
crate-type = ["rlib", "cdylib"]   # cdylib needed for Android .so

[dependencies]
yrs = "0.27.0"
serde_json = "*"
rand = "0.10.1"
uniffi = { version = "0.32", features = ["cli"] }
