## Friday, September 25, 2026

DONE
consts + getters in every tbl_* file.
key_value_storage.rs renamed to tbl_key_value_storage.rs.
macro_rules for const getters built and tested in src/macro_explained.
BREAKING_CHANGES.md written.

TODO

move every new_*_row helper out of new_row_helper.rs
into the tbl_* file it belongs to.

rename in every tbl_* file:
  x_columns() -> new_table_x()
  new_x_row() -> new_row_x()

update every caller of those names.

delete the empty macro_explained/ at the crate root
(the real one is src/macro_explained/).

delete new_row_helper.rs once its contents have moved.

SEPARATE PROJECT

col unbuilder / lookup tables built into the binary.

caller hands back the const it queried with. the unbuilder
looks up that column's ColumnDef, sees its type, and returns
the matching Col variant. caller stops guessing the return type.

NOTE

the col unbuilder thing was originally going to be a
mini project in the rust_practice repo, to mess around with
lookup tables built into the binary. probably not doing that
now, something else came up.
